{
  "name": "@slashmon/backend",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/main.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "for file in src/*.mjs scripts/*.mjs test/*.mjs; do node --check \"$file\" || exit 1; done"
  },
  "dependencies": {
    "viem": "^2.55.4",
    "web-push": "^3.6.7"
  }
}
